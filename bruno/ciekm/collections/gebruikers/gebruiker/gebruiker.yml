info:
  name: gebruiker
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/gebruiker"
  body:
    type: json
    data: |-
      {
        "naam": "el tigre",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
