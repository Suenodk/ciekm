info:
  name: create-gebruiker
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/gebruiker"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
