info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/identity/register"
  body:
    type: json
    data: |-
      {
        "naam": "el tigre",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
