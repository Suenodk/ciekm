info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/identity/login"
  body:
    type: json
    data: |-
      {
        "naam": "el tigre",
        "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
